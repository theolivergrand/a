# 🎯 Desktop UI/UX Analyzer v2.0

Интеллектуальный анализатор пользовательских интерфейсов с модульной архитектурой, построенный на базе Google Gemini AI и PyQt6.

## ✨ Возможности

- 🔍 **Автоматическое обнаружение UI элементов** - ИИ находит кнопки, поля ввода, меню и другие интерактивные элементы
- 📊 **Интерактивная визуализация** - наведите курсор на элементы для получения подробной информации
- 🎨 **Современный интерфейс** - красивый и удобный интерфейс на PyQt6
- 📱 **Адаптивный дизайн** - масштабируемый интерфейс с поддержкой High DPI
- 🔢 **Нумерация элементов** - каждый элемент имеет уникальный номер для удобства
- 🏗️ **Модульная архитектура** - чистый код с разделением ответственности
- 📤 **Экспорт результатов** - сохранение в JSON и TXT форматах
- 🧪 **Тестирование** - покрытие кода автоматическими тестами

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd desktop-analyzer
```

### 2. Установка зависимостей

```bash
cd desktop_analyzer
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Скопируйте файл примера конфигурации:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните ваши настройки:

```env
# Google Cloud Project Configuration
GCP_PROJECT_ID=your-actual-gcp-project-id
GCP_SECRET_ID=your-secret-manager-secret-id

# Или используйте прямой API ключ (не рекомендуется для продакшена)
# GEMINI_API_KEY=your-gemini-api-key-here
```

### 4. Настройка Google Cloud (рекомендуемый способ)

1. Создайте проект в Google Cloud Console
2. Включите Vertex AI API
3. Создайте секрет в Secret Manager с вашим Gemini API ключом
4. Настройте аутентификацию (service account или gcloud auth)

### 5. Запуск приложения

```bash
python app.py
```

Или через Make:

```bash
make run
```

## 🛠️ Архитектура проекта

Проект полностью рефакторен с применением принципов SOLID и чистой архитектуры:

```text
desktop_analyzer/
├── app.py                     # Точка входа приложения
├── config/                    # Конфигурация
│   ├── __init__.py
│   └── config.py             # Управление конфигурацией
├── src/                      # Исходный код
│   ├── models/               # Модели данных
│   │   ├── __init__.py
│   │   └── ui_element.py     # UIElement, AnalysisResult
│   ├── services/             # Бизнес-логика
│   │   ├── __init__.py
│   │   └── ai_service.py     # AI анализ с Google Gemini
│   ├── controllers/          # Контроллеры
│   │   ├── __init__.py
│   │   └── app_controller.py # Основной контроллер
│   ├── views/                # UI компоненты
│   │   ├── __init__.py
│   │   ├── main_window.py    # Главное окно
│   │   └── image_canvas.py   # Канвас для изображений
│   └── utils/                # Утилиты
│       ├── __init__.py
│       └── file_utils.py     # Работа с файлами
├── tests/                    # Автоматические тесты
│   └── test_basic.py         # Базовые тесты
├── requirements.txt          # Python зависимости
├── .env.example             # Пример конфигурации
└── main.py                  # Старый файл (deprecated)
```

## 🔧 Конфигурация

### Переменные среды

Все настройки управляются через переменные среды в файле `.env`:

- `GCP_PROJECT_ID` - ID вашего Google Cloud проекта
- `GCP_SECRET_ID` - ID секрета в Secret Manager
- `GEMINI_MODEL` - Модель Gemini (по умолчанию: gemini-1.5-flash)
- `GEMINI_API_KEY` - Прямой API ключ (альтернатива Secret Manager)
- `APP_TITLE` - Заголовок приложения
- `WINDOW_WIDTH`, `WINDOW_HEIGHT` - Размеры окна
- `DEBUG` - Режим отладки
- `LOG_LEVEL` - Уровень логирования

### Альтернативная настройка с прямым API ключом

Если вы не хотите использовать Google Cloud Secret Manager, можете указать API ключ напрямую в `.env`:

```env
GEMINI_API_KEY=your-gemini-api-key-here
```

**⚠️ Внимание:** Никогда не коммитьте файл `.env` с реальными API ключами в публичный репозиторий!

## 📖 Использование

1. Запустите приложение: `python app.py`
2. Нажмите "Выбрать изображение" или используйте Ctrl+O
3. Загрузите скриншот пользовательского интерфейса
4. Нажмите "Анализировать UI" или используйте Ctrl+A
5. Дождитесь завершения анализа
6. Изучите результаты:
   - **Список элементов** - список всех найденных UI элементов
   - **Интерактивная визуализация** - наведите курсор на зеленые области
   - **Детали элемента** - подробная информация о выбранном элементе
7. Экспортируйте результаты в JSON или TXT

## 🎨 Особенности интерфейса

- 🖱️ **Интерактивные элементы** - кликайте и наводите курсор на найденные элементы
- 💡 **Подробная информация** - детальные сведения о каждом элементе
- 🎯 **Визуальная индикация** - цветовое выделение выбранных и активных элементов
- ✨ **Современный дизайн** - чистый и интуитивный интерфейс
- ⌨️ **Горячие клавиши** - быстрый доступ к основным функциям

## 🧪 Тестирование

Запуск тестов:

```bash
cd desktop_analyzer
python -m pytest tests/ -v
```

Или через Make:

```bash
make test
```

## 📊 Make команды

Проект включает Makefile с полезными командами:

```bash
make help          # Показать все доступные команды
make install       # Установить зависимости
make run           # Запустить приложение
make test          # Запустить тесты
make lint          # Проверить код
make ctags         # Сгенерировать теги
make stats         # Показать статистику проекта
```

## 🔒 Безопасность

- Все секретные данные исключены из Git репозитория через `.gitignore`
- Используется Google Cloud Secret Manager для безопасного хранения API ключей
- Конфигурационные файлы с секретами не попадают в репозиторий
- Логирование не содержит чувствительных данных

## 🏗️ Архитектурные принципы

- **SOLID принципы** - каждый класс имеет единственную ответственность
- **MVC паттерн** - разделение моделей, представлений и контроллеров
- **Dependency Injection** - слабая связанность компонентов
- **Event-driven** - использование сигналов PyQt для коммуникации
- **Конфигурируемость** - все настройки через переменные среды
- **Тестируемость** - код покрыт автоматическими тестами

## 🚧 Что изменилось в v2.0

### ✅ Улучшения архитектуры

- ✅ Разделение кода на модули по принципу единственной ответственности
- ✅ Внедрение паттерна MVC/MVP
- ✅ Вынос конфигурации в отдельную систему
- ✅ Создание переиспользуемых компонентов
- ✅ Добавление автоматических тестов

### ✅ Улучшения пользовательского интерфейса

- ✅ Модернизация до PyQt6
- ✅ Улучшенная интерактивность
- ✅ Более информативные сообщения об ошибках
- ✅ Горячие клавиши
- ✅ Прогресс-бар для анализа

### ✅ Улучшения разработки

- ✅ Структурированная система логирования
- ✅ Обработка ошибок на всех уровнях
- ✅ Документация кода
- ✅ Make команды для автоматизации
- ✅ Гибкая система конфигурации

## 🚧 Что изменилось в v2.1

### 🔧 Технические улучшения

- 🏷️  **Индексация исходников** — добавлена автоматическая генерация тегов через `make ctags` и скрипты в `ctags_files/`.
- 📚 **Документация** — создан файл `docs/plan_v2.md` с планом релиза; README дополнен новыми изменениями.
- 🚦 **CI-проверки** — обновлены команды `make lint` и `make test` для дополнительной валидации кода.

### 🎨 Пользовательский интерфейс

- 🎛️ **Мелкие правки UI** — уточнены размеры областей выделения на канвасе.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Следуйте архитектурным принципам проекта
4. Добавьте тесты для новой функциональности
5. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
6. Отправьте в ветку (`git push origin feature/amazing-feature`)
7. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте, что все зависимости установлены: `make install`
2. Убедитесь, что `.env` создан и заполнен правильно
3. Проверьте настройки Google Cloud
4. Запустите тесты: `make test`
5. Проверьте логи: `app.log`
6. Создайте Issue в репозитории с подробным описанием проблемы

## 🔄 Дорожная карта

Планы развития проекта:

- 🔮 **Поддержка новых AI моделей** - интеграция с Claude, GPT-4V
- 🎯 **Улучшенное обнаружение** - более точные алгоритмы анализа
- 🌐 **Веб-интерфейс** - дополнительный веб-интерфейс на Flask/FastAPI
- 📱 **Мобильные UI** - специализированный анализ мобильных интерфейсов
- 🔄 **Batch обработка** - анализ множества изображений
- 🎨 **Темы интерфейса** - настраиваемое оформление
- 🔌 **Система плагинов** - расширяемая архитектура
- 📊 **Аналитика** - статистика и отчеты по анализу